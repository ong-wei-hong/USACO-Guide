#include <bits/stdc++.h>
using namespace std;

int main(){
  int m, n, k;
  cin>>m>>n>>k;
  for(int i=0; i<m; ++i){
    string s;
    cin>>s;
    for(int j=0; j<m; ++j)
      for(int a=0; a<k; ++a)
	cout<<s[j];
    cout<<endl;
  }
}
