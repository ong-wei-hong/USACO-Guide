#include <bits/stdc++.h>
using namespace std;

int main(){
  freopen("blocks.in", "r", stdin);
  freopen("blocks.out", "w", stdout);
  int n;
  cin>>n;
  array<int, 26> ans;
  while(--n){
    string s1, s2;
    cin>>s1>>s2;
    array<int, 26> freq1{}, freq2{};
    
  }
}
